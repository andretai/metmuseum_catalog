import { ExtractContentPipe } from './extract-content.pipe';

describe('ExtractContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractContentPipe();
    expect(pipe).toBeTruthy();
  });
});
